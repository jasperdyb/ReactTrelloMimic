type Query {
  info: String!
  list: [Todo!]!
  todo(id: ID!): Todo
}

type Mutation {
  addTodo(name: String!): Todo!
  updateTodo(id: ID!, name: String): Todo
  deleteTodo(id: ID!): Todo
}

type Todo {
  id: ID!
  name: String!
  finished: Boolean!
}
